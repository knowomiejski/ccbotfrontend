import React from 'react';
import {observer} from "mobx-react-lite";

const QuestionsBotPage = () => {
    return (
        <div>
            questions bot
        </div>
    );
};

export default observer(QuestionsBotPage);
